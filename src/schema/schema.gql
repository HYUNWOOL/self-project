type User {
  id: Int!
  name: String!
  email: String!
  password: String!
  age: Int!
}

type Query {
  users: [User]!,
  user: User!
}

type Mutation {
  createUser(name: String!, email: String!, password: String!): User!
}
